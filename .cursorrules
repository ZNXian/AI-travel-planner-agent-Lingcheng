# Lingcheng 项目开发规则

## 1. 虚拟环境
- 项目必须在根目录下使用 Python 自带的 venv 创建虚拟环境，文件夹名称为 `venv`。
- 所有运行、安装依赖的命令都需要在本venv虚拟环境下进行。
- 不要在代码中自动创建 venv，只需在 README 中写明激活步骤。

## 2. 文件头部注释
每个 `.py` 文件顶部必须用三引号包含以下信息：
- 文件名
- 本文件的用途说明（一两句话）
- 对外暴露的函数、类或变量（名称及简短用途）

示例：
"""
文件名: graph.py
用途: 定义 LangGraph 状态机，编排旅游规划 Agent 的所有节点。
对外暴露: create_agent_graph, AgentState
"""

## 3. 函数注释
每个自定义函数（包括私有函数）必须在其定义下方用三引号写一句简短注释，说明该函数的功能和关键参数（如果有）。
不需要详细的参数列表和返回值类型（除非特别复杂），但必须让阅读者明白这个函数“做什么”。
示例：
def fetch_weather(city: str):
    """根据城市名调用 API 获取当前天气情况。"""
    pass

## 4. 其他要求
- 错误处理：外部 API/网络调用需要 try-except，返回友好错误。
- 敏感信息从环境变量.env读取，并且禁止将敏感信息输出到终端（可以用变量名代替）。
- 导入按标准库、第三方、本地模块分组。

请严格按照以上规则生成所有代码，无需额外提醒。
